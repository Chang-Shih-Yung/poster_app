# macOS
.DS_Store

# Node — any nested node_modules + vite/vitest cache that may land at root
# when commands run from repo root instead of admin/.
node_modules/
.vite/

# Flutter / Dart
.dart_tool/
.packages
.pub-cache/
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies
.metadata
pubspec.lock
# IDE
.idea/
.vscode/
*.iml
# Firebase (config files are NOT secrets, keep them in repo)
.firebase/
firebase-debug.log
.firebaserc.local
# Env
.env
.env.*
# Supabase local state
supabase/.temp/
supabase/.branches/
.gstack/

# Claude / agent local state (worktrees, scheduled tasks, settings)
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/launch.json
.vercel
_flutter/
